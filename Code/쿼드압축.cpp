#include <string>
#include <vector>
#include <cmath>

using namespace std;

vector<int> solution(vector<vector<int>> arr) {
   vector<int> answer;
   int squareSize = 0;
   bool canGroup = true;

   //배열 묶음 만들기
   squareSize = sqrt(arr.size());
   //x와 Y좌표로 트래킹
   //방문 표시로 더 방문하지 않아도 되는 구역 표시 
   //isCheck


  

   return answer;
}